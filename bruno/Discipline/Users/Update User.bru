meta {
  name: Update User
  type: http
  seq: 4
}

put {
  url: {{hostname}}{{api_prefix}}/{{api_version}}/users/{{user_id}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "Chunk",
    "email": "updated@example.com",
    "role": "player",
    "password_hash": "newhashedpassword123"
  }
}

settings {
  encodeUrl: true
}
