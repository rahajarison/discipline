meta {
  name: Get User Matches
  type: http
  seq: 6
}

get {
  url: {{hostname}}{{api_prefix}}/{{api_version}}/users/{{user_id}}/matches
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}
